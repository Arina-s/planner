CREATE TABLE goal (
    id serial PRIMARY KEY,
    name VARCHAR(255),
    priority VARCHAR(20),
    estimation_id INT
);

CREATE TABLE estimation (
    id serial PRIMARY KEY,
    duration VARCHAR(255),
    complexity INT
);
